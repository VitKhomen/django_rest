

# ⚙️ Основні налаштування Django
SECRET_KEY=your-secret-key              # Секретний ключ Django (згенеруй за допомогою django-secret-key-generator)
DEBUG=True                              # Режим відладки (True для розробки, False для продакшена)

# 🗄️ База даних
DATABASE_URL=postgresql://user:password@host:port/dbname   # Рядок підключення до бази (наприклад, Supabase або Render PostgreSQL)

# 📧 Налаштування SMTP (для надсилання листів: реєстрація, відновлення паролю тощо)
EMAIL_HOST_USER=your-email@example.com   # Пошта-відправник (повна адреса e-mail)
EMAIL_HOST_PASSWORD=your-smtp-app-key    # Пароль додатку / SMTP API key (НЕ звичайний пароль від пошти!)


# 👤 Дані суперкористувача (для автостворення при деплої)
DJANGO_SUPERUSER_USERNAME=admin          # Логін суперкористувача
DJANGO_SUPERUSER_PASSWORD=adminpassword  # Пароль суперкористувача
DJANGO_SUPERUSER_EMAIL=admin@example.com # Email суперкористувача

# ☁️ Cloudinary (зберігання зображень)
CLOUDINARY_CLOUD_NAME=your-cloud-name    # Назва акаунта Cloudinary
CLOUDINARY_API_KEY=your-api-key          # API Key з Cloudinary Dashboard
CLOUDINARY_API_SECRET=your-api-secret    # API Secret з Cloudinary Dashboard